

// HW3P3 Instruction - what to add to slist.h

// The copy constructor to allow pass by value and return
// The regular copy constructor does not work well with pointers
slist(const slist&);

// overloading =
// The operator = does not work well with pointers
slist& operator=(const slist&);

// The code to put in the implementation file are in Notes-7A

// Extra credit is to overload == which does not work with Objects